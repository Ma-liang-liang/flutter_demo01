name: dio_network
description: "基于 Dio 的业务层网络封装，支持统一配置、公参、公共Header、业务码拦截、泛型字典转模型、日志开关等。"
version: 0.0.1
homepage:

environment:
  sdk: ^3.12.0
  flutter: '>=3.44.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  # 纯 Dart package，无原生代码
